{
  "bin": {
    "llms-audit": "./src/cli.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1"
  },
  "description": "Audit any site for the AI-answer era: llms.txt, schema, sitemap → score + fix list.",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "license": "MIT",
  "name": "llms-audit",
  "scripts": {
    "demo-fixture": "bun fixtures/server.ts",
    "test": "bun test"
  },
  "version": "0.1.0"
}