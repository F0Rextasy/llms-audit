# Changelog

## [0.1.0] - 2026-09-25

- First release: 8 weighted AI-answer-readiness checks (llms.txt, llms-full,
  sitemap, robots AI crawlers, JSON-LD, meta, h1, HTTPS) with score + fixes,
  `--json`, `--out report.html`, `--fix-llms` template generator, and a stdio
  MCP server (`--mcp`, tool `audit_url`).
